<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="koggiri.task.mapper.TaskMapper">
	<cache />

	<insert id="insertTask" parameterType="Task">
		insert into task(
		ta_seq, emp_id, ta_startdate, ta_enddate, ta_weekresult,
		ta_nextresult, ta_regdate, ta_hitcount
		)
		values(
		TASK_TA_SEQ.nextval, '12', #{ta_startdate} , #{ta_enddate}, #{ta_weekresult},
		#{ta_nextresult}, SYSDATE, 0
		)
	</insert>

	<select id="listTask" resultType="Task" >
		select *from Task
	</select>

</mapper>