<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="koggiri.task.mapper.TaskMapper">
	<cache />

	<insert id="insertTask" parameterType="Task">
		insert into task(
		ta_seq, emp_id, ta_startdate, ta_enddate, ta_weekresult,
		ta_nextresult, ta_regdate, ta_hitcount
		)
		values(
		TASK_TA_SEQ.nextval, #{emp_id}, #{ta_startdate} , #{ta_enddate}, #{ta_weekresult},
		#{ta_nextresult}, SYSDATE, 0
		)
	</insert>

	<select id="listTask" resultType="Task" >
		select *from Task
	</select>
	
      <select id="detailTask" resultType="Task" parameterType="int">
		select * from Task where ta_seq = #{ta_seq}
	</select>
	
	<update id="updateTask" parameterType="Task">
		update Task set 
			ta_startdate = #{ta_startdate}, 
			ta_enddate = #{ta_enddate},
			ta_weekresult = #{ta_weekresult},
			ta_nextresult = #{ta_nextresult}
		     where ta_seq = #{ta_seq}
	</update>
	
	<delete id="deleteTask" parameterType="Task">
		delete from Task where ta_seq = #{ta_seq}
	</delete>
	
	<update id="updateTask_Hit" parameterType="int">
	    update Task set
			ta_hitcount = ta_hitcount + 1
			where ta_seq = #{ta_seq}
    </update>
    
    	<select id="countTask" resultType="int" parameterType="TaskSearch">
		select count(*) from Task
		<if test="area != null">
			<where>
				<foreach collection="area" item="item" separator= "OR" open = "(" close = ")">
					${item} LIKE #{searchKey}
				</foreach>
			</where>
		</if>
	</select>
</mapper>